/**
 * TEST HUBS — NOT APPROVED FOR PRODUCTION
 *
 * Mappers in this package are under evaluation and have not been approved
 * for inclusion in the DPLA production index. Each hub in this package
 * must also have `<hub>.status = test` set in i3.conf.
 *
 * See docs/ingestion/README_TEST_HUBS.md for full conventions.
 */
package dpla.ingestion3.mappers.providers

package object experimental {}
